# Progress Bar Prototype (Static Demo)

This folder is deployed to GitHub Pages. Use the **Demo** button to see the progress bar in action (the proxy server is not available on static hosting).

For full functionality with real URLs and network throttling, run locally:

```bash
cd prototype && npm install && npm start
```
